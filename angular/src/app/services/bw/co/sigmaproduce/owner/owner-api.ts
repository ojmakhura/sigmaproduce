// Generated by andromda-angular cartridge (service\service.impl.ts.vsl) CAN EDIT
import { Injectable, inject } from '@angular/core';
import { Observable } from 'rxjs';
import { OwnerDTO } from '@models/bw/co/sigmaproduce/owner/owner-dto';
import { OwnerListDTO } from '@models/bw/co/sigmaproduce/owner/owner-list-dto';
import { HttpClient } from '@angular/common/http';
import { Page } from '@models/page.model';
import { SearchObject } from '@models/search-object';

@Injectable({
  providedIn: 'root'
})
export class OwnerApi {
    
    protected path = '/owner';

    private http = inject(HttpClient);

    public findById(id: string): Observable<OwnerDTO> {

        return this.http.get<OwnerDTO>(`${this.path}/${id}`);
    }

    public getAll(): Observable<OwnerListDTO[]> {

        return this.http.get<OwnerListDTO[]>(`${this.path}`);
    }

    public remove(id: string): Observable<boolean> {

        return this.http.delete<boolean>(`${this.path}/${id}`);
    }

    public save(owner: OwnerDTO): Observable<OwnerDTO> {

        return this.http.post<OwnerDTO>(`${this.path}`, owner);
    }

    public search(criteria: string): Observable<OwnerListDTO[]> {

        return this.http.post<OwnerListDTO[]>(`${this.path}/search?criteria=${criteria}`, criteria);
    }

}

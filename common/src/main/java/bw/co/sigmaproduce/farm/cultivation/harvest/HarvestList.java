// license-header java merge-point
//
/**
 * @author Generated by ValueObject.vsl in andromda-java-cartridge on 06/12/2024 19:31:22+0200 Do not modify by hand!
 *
 * TEMPLATE:     ValueObject.vsl in andromda-java-cartridge.
 * MODEL CLASS:  AndroMDAModel::backend::bw.co.sigmaproduce::farm::cultivation::harvest::HarvestList
 * STEREOTYPE:   ValueObject
 */
package bw.co.sigmaproduce.farm.cultivation.harvest;

import java.io.Serializable;
import java.time.LocalDate;
import java.util.Arrays;
import org.apache.commons.lang3.builder.CompareToBuilder;
import org.apache.commons.lang3.builder.EqualsBuilder;
import org.apache.commons.lang3.builder.HashCodeBuilder;
import org.apache.commons.lang3.builder.ToStringBuilder;

/**
 * TODO: Model Documentation for HarvestList
 */
public class HarvestList
    implements Serializable, Comparable<HarvestList>
{
    /** The serial version UID of this class. Needed for serialization. */
    private static final long serialVersionUID = 880003662991714780L;

    // Class attributes
    /**
     * TODO: Model Documentation for HarvestList.id
     */
    protected String id;
    /**
     * TODO: Model Documentation for
     * HarvestList.harvestDate
     */
    protected LocalDate harvestDate;
    /**
     * TODO: Model Documentation for
     * HarvestList.quantity
     */
    protected Integer quantity = 0;
    /**
     * TODO: Model Documentation for HarvestList.unit
     */
    protected HarvestUnit unit;
    /**
     * TODO: Model Documentation for HarvestList.crop
     */
    protected String crop;
    /**
     * TODO: Model Documentation for HarvestList.farm
     */
    protected String farm;

    /** Default Constructor with no properties */
    public HarvestList()
    {
        // Documented empty block - avoid compiler warning - no super constructor
    }

    /**
     * Constructor taking only required properties
     * @param harvestDateIn LocalDate TODO: Model Documentation for HarvestList.harvestDate
     * @param unitIn HarvestUnit TODO: Model Documentation for HarvestList.unit
     */
    public HarvestList(final LocalDate harvestDateIn, final HarvestUnit unitIn)
    {
        this.harvestDate = harvestDateIn;
        this.unit = unitIn;
    }

    /**
     * Constructor with all properties
     * @param idIn String
     * @param harvestDateIn LocalDate
     * @param quantityIn Integer
     * @param unitIn HarvestUnit
     * @param cropIn String
     * @param farmIn String
     */
    public HarvestList(final String idIn, final LocalDate harvestDateIn, final Integer quantityIn, final HarvestUnit unitIn, final String cropIn, final String farmIn)
    {
        this.id = idIn;
        this.harvestDate = harvestDateIn;
        this.quantity = quantityIn;
        this.unit = unitIn;
        this.crop = cropIn;
        this.farm = farmIn;
    }

    /**
     * Copies constructor from other HarvestList
     *
     * @param otherBean Cannot be <code>null</code>
     * @throws NullPointerException if the argument is <code>null</code>
     */
    public HarvestList(final HarvestList otherBean)
    {
        this.id = otherBean.getId();
        this.harvestDate = otherBean.getHarvestDate();
        this.quantity = otherBean.getQuantity();
        this.unit = otherBean.getUnit();
        this.crop = otherBean.getCrop();
        this.farm = otherBean.getFarm();
    }

    /**
     * Copies all properties from the argument value object into this value object.
     * @param otherBean Cannot be <code>null</code>
     */
    public void copy(final HarvestList otherBean)
    {
        if (null != otherBean)
        {
            this.setId(otherBean.getId());
            this.setHarvestDate(otherBean.getHarvestDate());
            this.setQuantity(otherBean.getQuantity());
            this.setUnit(otherBean.getUnit());
            this.setCrop(otherBean.getCrop());
            this.setFarm(otherBean.getFarm());
        }
    }

    /**
     * TODO: Model Documentation for HarvestList.id
     * Get the id Attribute
     * @return id String
     */
    public String getId()
    {
        return this.id;
    }

    /**
     * TODO: Model Documentation for HarvestList.id
     * @param value String
     */
    public void setId(final String value)
    {
        this.id = value;
    }

    /**
     * TODO: Model Documentation for
     * HarvestList.harvestDate
     * Get the harvestDate Attribute
     * @return harvestDate LocalDate
     */
    public LocalDate getHarvestDate()
    {
        return this.harvestDate;
    }

    /**
     * TODO: Model Documentation for
     * HarvestList.harvestDate
     * @param value LocalDate
     */
    public void setHarvestDate(final LocalDate value)
    {
        this.harvestDate = value;
    }

    /**
     * TODO: Model Documentation for
     * HarvestList.quantity
     * Get the quantity Attribute
     * @return quantity Integer
     */
    public Integer getQuantity()
    {
        return this.quantity;
    }

    /**
     * TODO: Model Documentation for
     * HarvestList.quantity
     * @param value Integer
     */
    public void setQuantity(final Integer value)
    {
        this.quantity = value;
    }

    /**
     * TODO: Model Documentation for HarvestList.unit
     * Get the unit Attribute
     * @return unit HarvestUnit
     */
    public HarvestUnit getUnit()
    {
        return this.unit;
    }

    /**
     * TODO: Model Documentation for HarvestList.unit
     * @param value HarvestUnit
     */
    public void setUnit(final HarvestUnit value)
    {
        this.unit = value;
    }

    /**
     * TODO: Model Documentation for HarvestList.crop
     * Get the crop Attribute
     * @return crop String
     */
    public String getCrop()
    {
        return this.crop;
    }

    /**
     * TODO: Model Documentation for HarvestList.crop
     * @param value String
     */
    public void setCrop(final String value)
    {
        this.crop = value;
    }

    /**
     * TODO: Model Documentation for HarvestList.farm
     * Get the farm Attribute
     * @return farm String
     */
    public String getFarm()
    {
        return this.farm;
    }

    /**
     * TODO: Model Documentation for HarvestList.farm
     * @param value String
     */
    public void setFarm(final String value)
    {
        this.farm = value;
    }

    /**
     * @param object to compare this object against
     * @return boolean if equal
     * @see Object#equals(Object)
     */
    @Override
    public boolean equals(final Object object)
    {
        if (object==null || object.getClass() != this.getClass())
        {
             return false;
        }
        // Check if the same object instance
        if (object==this)
        {
            return true;
        }
        HarvestList rhs = (HarvestList) object;
        return new EqualsBuilder()
            .append(this.getId(), rhs.getId())
            .append(this.getHarvestDate(), rhs.getHarvestDate())
            .append(this.getQuantity(), rhs.getQuantity())
            .append(this.getUnit(), rhs.getUnit())
            .append(this.getCrop(), rhs.getCrop())
            .append(this.getFarm(), rhs.getFarm())
            .isEquals();
    }

    /**
     * @param object to compare this object against
     * @return int if equal
     * @see Comparable#compareTo(Object)
     */
    public int compareTo(final HarvestList object)
    {
        if (object==null)
        {
            return -1;
        }
        // Check if the same object instance
        if (object==this)
        {
            return 0;
        }
        return new CompareToBuilder()
            .append(this.getId(), object.getId())
            .append(this.getHarvestDate(), object.getHarvestDate())
            .append(this.getQuantity(), object.getQuantity())
            .append(this.getUnit(), object.getUnit())
            .append(this.getCrop(), object.getCrop())
            .append(this.getFarm(), object.getFarm())
            .toComparison();
    }

    /**
     * @return int hashCode value
     * @see Object#hashCode()
     */
    @Override
    public int hashCode()
    {
        return new HashCodeBuilder(1249046965, -82296885)
            .append(this.getId())
            .append(this.getHarvestDate())
            .append(this.getQuantity())
            .append(this.getUnit())
            .append(this.getCrop())
            .append(this.getFarm())
            .toHashCode();
    }

    /**
     * @return String representation of object
     * @see Object#toString()
     */
    @Override
    public String toString()
    {

        StringBuilder builder = new StringBuilder();
        builder.append("{\n");
        builder.append("\t\"id\" : \"" + this.getId() + "\"");
        builder.append("\t,\"harvestDate\" : \"" + this.getHarvestDate() + "\"");
        builder.append("\t,\"quantity\" : \"" + this.getQuantity() + "\"");
        builder.append("\t,\"unit\" : \"" + this.getUnit() + "\"");
        builder.append("\t,\"crop\" : \"" + this.getCrop() + "\"");
        builder.append("\t,\"farm\" : \"" + this.getFarm() + "\"");
        builder.append("}");
        return builder.toString();

    }

    /**
     * Compares the properties of this instance to the properties of the argument. This method will return
     * {@code false} as soon as it detects that the argument is {@code null} or not of the same type as
     * (or a sub-type of) this instance's type.
     *
     * <p/>For array, collection or map properties the comparison will be done one level deep, in other words:
     * the elements will be compared using the {@code equals()} operation.
     *
     * <p/>Note that two properties will be considered equal when both values are {@code null}.
     *
     * @param thatObject the object containing the properties to compare against this instance
     * @return this method will return {@code true} in case the argument has the same type as this class, or is a
     *      sub-type of this class and all properties as found on this class have equal values when queried on that
     *      argument instance; in all other cases this method will return {@code false}
     */
    public boolean equalProperties(final Object thatObject)
    {
        if (thatObject == null || !this.getClass().isAssignableFrom(thatObject.getClass()))
        {
            return false;
        }

        final HarvestList that = (HarvestList)thatObject;
        return
            equal(this.getId(), that.getId())
            && equal(this.getHarvestDate(), that.getHarvestDate())
            && equal(this.getQuantity(), that.getQuantity())
            && equal(this.getUnit(), that.getUnit())
            && equal(this.getCrop(), that.getCrop())
            && equal(this.getFarm(), that.getFarm())
        ;
    }

    /**
     * This is a convenient helper method which is able to detect whether or not two values are equal. Two values
     * are equal when they are both {@code null}, are arrays of the same length with equal elements or are
     * equal objects (this includes {@link java.util.Collection} and {@link java.util.Map} instances).
     *
     * <p/>Note that for array, collection or map instances the comparison runs one level deep.
     *
     * @param first the first object to compare, may be {@code null}
     * @param second the second object to compare, may be {@code null}
     * @return this method will return {@code true} in case both objects are equal as explained above;
     *      in all other cases this method will return {@code false}
     */
    protected static boolean equal(final Object first, final Object second)
    {
        final boolean equal;

        if (first == null)
        {
            equal = (second == null);
        }
        else if (first.getClass().isArray() && (second != null) && second.getClass().isArray())
        {
            equal = Arrays.equals((Object[])first, (Object[])second);
        }
        else // note that the following also covers java.util.Collection and java.util.Map
        {
            equal = first.equals(second);
        }

        return equal;
    }

    // HarvestList value-object java merge-point
}
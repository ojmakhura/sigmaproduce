// license-header java merge-point
//
// Attention: Generated code! Do not modify by hand!
// Generated by SpringServiceBase.vsl in andromda-spring-cartridge on $springUtils.date. Do not modify by hand!.
//
/**
 * TEMPLATE:    SpringServiceBase.vsl in andromda-spring cartridge
 * MODEL CLASS: AndroMDAModel::backend::bw.co.sigmaproduce::crop::variety::CropVarietyService
 * STEREOTYPE:  Service
 */
package bw.co.sigmaproduce.crop.variety;

import bw.co.sigmaproduce.PrincipalStore;
import java.security.Principal;
import java.util.Collection;
import java.util.Locale;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.context.MessageSource;
import org.springframework.data.domain.Page;

/**
 * <p>
 * Spring Service base class for <code>CropVarietyService</code>,
 * provides access to all services and entities referenced by this service.
 * </p>
 *
 * @see CropVarietyService
 */
public abstract class CropVarietyServiceBase
    implements CropVarietyService
{
    protected Logger logger = LoggerFactory.getLogger(CropVarietyService.class);
    protected final CropVarietyDao cropVarietyDao;
    protected final CropVarietyRepository cropVarietyRepository;
    protected final MessageSource messageSource;

    public CropVarietyServiceBase(
        CropVarietyDao cropVarietyDao,
        CropVarietyRepository cropVarietyRepository,
        MessageSource messageSource
    ) {
        this.cropVarietyDao = cropVarietyDao;
        this.cropVarietyRepository = cropVarietyRepository;
        this.messageSource = messageSource;
    }

	/**
     * Gets the reference to <code>cropVariety</code>'s DAO.
     * @return cropVarietyDao
     */
    protected CropVarietyDao getCropVarietyDao()
    {
        return this.cropVarietyDao;
    }

    /**
     * Gets the reference to <code>cropVariety</code>'s repository.
     * @return cropVarietyRepository
     */
    public CropVarietyRepository getCropVarietyRepository() {
        return cropVarietyRepository;
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public CropVarietyVO findById(final String id)
    {
        if (id == null || id.trim().length() == 0)
        {
            throw new IllegalArgumentException(
                "bw.co.sigmaproduce.crop.variety.CropVarietyService.findById(String id) - 'id' can not be null or empty");
        }
        try
        {
            return this.handleFindById(id);
        }
        catch (Throwable th)
        {
            throw new CropVarietyServiceException(
                "Error performing 'CropVarietyService.findById(String id)' --> " + th,
                th);
        }
    }

    /**
     * Performs the core logic for {@link #findById(String)}
     * @param id String TODO: Model Documentation for CropVarietyService.findById(id)
     * @return CropVarietyVO
     * @throws Exception
     */
    protected abstract CropVarietyVO handleFindById(String id)
        throws Exception;

    /**
     * {@inheritDoc}
     */
    @Override
    public Collection<CropVarietyList> getAll()
    {
        try
        {
            return this.handleGetAll();
        }
        catch (Throwable th)
        {
            throw new CropVarietyServiceException(
                "Error performing 'CropVarietyService.getAll()' --> " + th,
                th);
        }
    }

    /**
     * Performs the core logic for {@link #getAll()}
     * @return Collection<CropVarietyList>
     * @throws Exception
     */
    protected abstract Collection<CropVarietyList> handleGetAll()
        throws Exception;

    /**
     * {@inheritDoc}
     */
    @Override
    public Page<CropVarietyList> getAll(final Integer pageNumber, final Integer pageSize)
    {
        try
        {
            return this.handleGetAll(pageNumber, pageSize);
        }
        catch (Throwable th)
        {
            throw new CropVarietyServiceException(
                "Error performing 'CropVarietyService.getAll(Integer pageNumber, Integer pageSize)' --> " + th,
                th);
        }
    }

    /**
     * Performs the core logic for {@link #getAll(Integer, Integer)}
     * @param pageNumber Integer TODO: Model Documentation for CropVarietyService.getAll(pageNumber)
     * @param pageSize Integer TODO: Model Documentation for CropVarietyService.getAll(pageSize)
     * @return Page<CropVarietyList>
     * @throws Exception
     */
    protected abstract Page<CropVarietyList> handleGetAll(Integer pageNumber, Integer pageSize)
        throws Exception;

    /**
     * {@inheritDoc}
     */
    @Override
    public boolean remove(final String id)
    {
        if (id == null || id.trim().length() == 0)
        {
            throw new IllegalArgumentException(
                "bw.co.sigmaproduce.crop.variety.CropVarietyService.remove(String id) - 'id' can not be null or empty");
        }
        try
        {
            return this.handleRemove(id);
        }
        catch (Throwable th)
        {
            throw new CropVarietyServiceException(
                "Error performing 'CropVarietyService.remove(String id)' --> " + th,
                th);
        }
    }

    /**
     * Performs the core logic for {@link #remove(String)}
     * @param id String TODO: Model Documentation for CropVarietyService.remove(id)
     * @return boolean
     * @throws Exception
     */
    protected abstract boolean handleRemove(String id)
        throws Exception;

    /**
     * {@inheritDoc}
     */
    @Override
    public CropVarietyVO save(final CropVarietyVO cropVariety)
    {
        if (cropVariety == null)
        {
            throw new IllegalArgumentException(
                "bw.co.sigmaproduce.crop.variety.CropVarietyService.save(CropVarietyVO cropVariety) - 'cropVariety' can not be null");
        }
        if (cropVariety.getCrop() == null)
        {
            throw new IllegalArgumentException(
                "bw.co.sigmaproduce.crop.variety.CropVarietyService.save(CropVarietyVO cropVariety) - 'cropVariety.crop' can not be null");
        }
        if (cropVariety.getCode() == null || cropVariety.getCode().trim().length() == 0)
        {
            throw new IllegalArgumentException(
                "bw.co.sigmaproduce.crop.variety.CropVarietyService.save(CropVarietyVO cropVariety) - 'cropVariety.code' can not be null or empty");
        }
        if (cropVariety.getName() == null || cropVariety.getName().trim().length() == 0)
        {
            throw new IllegalArgumentException(
                "bw.co.sigmaproduce.crop.variety.CropVarietyService.save(CropVarietyVO cropVariety) - 'cropVariety.name' can not be null or empty");
        }
        try
        {
            return this.handleSave(cropVariety);
        }
        catch (Throwable th)
        {
            throw new CropVarietyServiceException(
                "Error performing 'CropVarietyService.save(CropVarietyVO cropVariety)' --> " + th,
                th);
        }
    }

    /**
     * Performs the core logic for {@link #save(CropVarietyVO)}
     * @param cropVariety CropVarietyVO TODO: Model Documentation for CropVarietyService.save(cropVariety)
     * @return CropVarietyVO
     * @throws Exception
     */
    protected abstract CropVarietyVO handleSave(CropVarietyVO cropVariety)
        throws Exception;

    /**
     * {@inheritDoc}
     */
    @Override
    public Collection<CropVarietyList> search(final String criteria)
    {
        try
        {
            return this.handleSearch(criteria);
        }
        catch (Throwable th)
        {
            throw new CropVarietyServiceException(
                "Error performing 'CropVarietyService.search(String criteria)' --> " + th,
                th);
        }
    }

    /**
     * Performs the core logic for {@link #search(String)}
     * @param criteria String TODO: Model Documentation for CropVarietyService.search(criteria)
     * @return Collection<CropVarietyList>
     * @throws Exception
     */
    protected abstract Collection<CropVarietyList> handleSearch(String criteria)
        throws Exception;

    /**
     * {@inheritDoc}
     */
    @Override
    public Page<CropVarietyList> search(final Integer pageNumber, final Integer pageSize, final String criteria)
    {
        if (criteria == null || criteria.trim().length() == 0)
        {
            throw new IllegalArgumentException(
                "bw.co.sigmaproduce.crop.variety.CropVarietyService.search(Integer pageNumber, Integer pageSize, String criteria) - 'criteria' can not be null or empty");
        }
        try
        {
            return this.handleSearch(pageNumber, pageSize, criteria);
        }
        catch (Throwable th)
        {
            throw new CropVarietyServiceException(
                "Error performing 'CropVarietyService.search(Integer pageNumber, Integer pageSize, String criteria)' --> " + th,
                th);
        }
    }

    /**
     * Performs the core logic for {@link #search(Integer, Integer, String)}
     * @param pageNumber Integer TODO: Model Documentation for CropVarietyService.search(pageNumber)
     * @param pageSize Integer TODO: Model Documentation for CropVarietyService.search(pageSize)
     * @param criteria String TODO: Model Documentation for CropVarietyService.search(criteria)
     * @return Page<CropVarietyList>
     * @throws Exception
     */
    protected abstract Page<CropVarietyList> handleSearch(Integer pageNumber, Integer pageSize, String criteria)
        throws Exception;

    /**
     * Gets the current <code>principal</code> if one has been set,
     * otherwise returns <code>null</code>.
     *
     * @return the current principal
     */
    protected Principal getPrincipal()
    {
        return PrincipalStore.get();
    }

    /**
     * Gets the message source available to this service.
     * @return MessageSource
     */
    protected MessageSource getMessages()
    {
        return messageSource;
    }

    /**
     * Gets the message having the given <code>key</code> in the underlying message bundle.
     *
     * @param key the key of the message in the messages.properties message bundle.
     * @return String
     */
    protected String getMessage(final String key)
    {
        return this.getMessages().getMessage(key, null, null);
    }

    /**
     * Gets the message having the given <code>key</code> and <code>arguments</code> in the
     * underlying message bundle.
     *
     * @param key the key of the message in the messages.properties message bundle.
     * @param arguments any arguments to substitute when resolving the message.
     * @return String
     */
    protected String getMessage(final String key, final Object[] arguments)
    {
        return this.getMessages().getMessage(key, arguments, null);
    }

    /**
     * Gets the message having the given <code>key</code> using the given <code>arguments</code>
     * for the given <code>locale</code>.
     *
     * @param key the key of the message in the messages.properties message bundle.
     * @param arguments any arguments to substitute when resolving the message.
     * @param locale the locale of the messages to retrieve.
     * @return String
     */
    protected String getMessage(
        final String key, final Object[] arguments,
        final Locale locale)
    {
        return this.getMessages().getMessage(key, arguments, locale);
    }


// spring-service-base merge-point
}
// license-header java merge-point
//
// Attention: Generated code! Do not modify by hand!
// Generated by hibernate/HibernateEntity.vsl in andromda-hibernate-cartridge on 06/12/2024 19:31:13+0200.
//
package bw.co.sigmaproduce.village;

import bw.co.sigmaproduce.district.District;
import jakarta.persistence.Column;
import jakarta.persistence.Entity;
import jakarta.persistence.FetchType;
import jakarta.persistence.GeneratedValue;
import jakarta.persistence.GenerationType;
import jakarta.persistence.Id;
import jakarta.persistence.JoinColumn;
import jakarta.persistence.ManyToOne;
import jakarta.persistence.NamedQuery;
import jakarta.persistence.Table;
import java.io.Serializable;

/**
 * TODO: Model Documentation for Village
 */
@Entity
@Table(name = "VILLAGE")
// Uncomment to enable caching for Village
// @org.hibernate.annotations.Cache(usage = org.hibernate.annotations.CacheConcurrencyStrategy.read-write)
@NamedQuery(name = "Village.findAll", query = "SELECT v FROM Village AS v")
// HibernateEntity.vsl annotations merge-point
public class Village
    implements Serializable, Comparable<Village>
{
    /**
     * The serial version UID of this class. Needed for serialization.
     */
    private static final long serialVersionUID = -8177849793595393083L;

    // Generate 3 attributes
    private String code;

    /**
     * TODO: Model Documentation for Village.code
     * @return this.code String
     */
    @Column(name = "CODE", unique = true, nullable = false, insertable = true, updatable = true)
    public String getCode()
    {
        return this.code;
    }

    /**
     * TODO: Model Documentation for Village.code
     * @param codeIn String
     */
    public void setCode(String codeIn)
    {
        this.code = codeIn;
    }

    private String name;

    /**
     * TODO: Model Documentation for Village.name
     * @return this.name String
     */
    @Column(name = "NAME", unique = true, nullable = false, insertable = true, updatable = true)
    public String getName()
    {
        return this.name;
    }

    /**
     * TODO: Model Documentation for Village.name
     * @param nameIn String
     */
    public void setName(String nameIn)
    {
        this.name = nameIn;
    }

    private Long id;

    /**
     * TODO: Model Documentation for Village.id
     * @return this.id Long
     */
    @Id
    @GeneratedValue(strategy = GenerationType.AUTO)
    @Column(name = "ID", unique = false, nullable = true, insertable = true, updatable = true)
    public Long getId()
    {
        return this.id;
    }

    /**
     * TODO: Model Documentation for Village.id
     * @param idIn Long
     */
    public void setId(Long idIn)
    {
        this.id = idIn;
    }

    // Generate 1 associations
    private District district;

    /**
     * TODO: Model Documentation for District
     * @return this.district District
     */
    @ManyToOne( fetch = FetchType.LAZY)
    @JoinColumn(name = "DISTRICT_FK")
    public District getDistrict()
    {
        return this.district;
    }

    /**
     * TODO: Model Documentation for District
     * @param districtIn District
     */
    public void setDistrict(District districtIn)
    {
        this.district = districtIn;
    }

    /**
     * Returns <code>true</code> if the argument is an Village instance and all identifiers for this entity
     * equal the identifiers of the argument entity. Returns <code>false</code> otherwise.
     */
    @Override
    public boolean equals(Object object)
    {
        if (this == object)
        {
            return true;
        }
        if (!(object instanceof Village))
        {
            return false;
        }
        final Village that = (Village)object;
        if (this.id == null || that.getId() == null || !this.id.equals(that.getId()))
        {
            return false;
        }
        return true;
    }

    /**
     * Returns a hash code based on this entity's identifiers.
     */
    @Override
    public int hashCode()
    {
        int hashCode = 0;
        hashCode = 29 * hashCode + (this.id == null ? 0 : this.id.hashCode());

        return hashCode;
    }

    /**
     * Constructs new instances of {@link Village}.
     */
    public static final class Factory
    {
        /**
         * Constructs a new instance of {@link Village}.
         * @return new Village()
         */
        public static Village newInstance()
        {
            return new Village();
        }

        /**
         * Constructs a new instance of {@link Village}, taking all required and/or
         * read-only properties as arguments, except for identifiers.
         * @param code String
         * @param name String
         * @return newInstance
         */
        public static Village newInstance(String code, String name)
        {
            final Village entity = new Village();
            entity.setCode(code);
            entity.setName(name);
            return entity;
        }

        /**
         * Constructs a new instance of {@link Village}, taking all possible properties
         * (except the identifier(s))as arguments.
         * @param code String
         * @param name String
         * @param district District
         * @return newInstance Village
         */
        public static Village newInstance(String code, String name, District district)
        {
            final Village entity = new Village();
            entity.setCode(code);
            entity.setName(name);
            entity.setDistrict(district);
            return entity;
        }
    }

    /**
     * @param other
     * @return a negative integer, zero, or a positive integer as this object is less than, equal to, or greater than the specified object.
     * @see Comparable#compareTo
     */
    @Override
    public int compareTo(Village other)
    {
        int cmp = 0;
        if (this.getId() != null)
        {
            cmp = this.getId().compareTo(other.getId());
        }
        else
        {
            if (this.getCode() != null)
            {
                cmp = (cmp != 0 ? cmp : this.getCode().compareTo(other.getCode()));
            }
            if (this.getName() != null)
            {
                cmp = (cmp != 0 ? cmp : this.getName().compareTo(other.getName()));
            }
        }
        return cmp;
    }

    /**
     * Returns a String representation of the object
     * @return String Textual representation of the object displaying name/value pairs for all attributes
     * @see Object#toString
     */
    @Override
    public String toString()
    {
        StringBuilder sb = new StringBuilder();
        sb.append("Village(");
        sb.append(" code=").append(getCode());
        sb.append(" name=").append(getName());
        sb.append(" id=").append(getId());
        sb.append(")");
        return sb.toString();
    }
// HibernateEntity.vsl merge-point
}
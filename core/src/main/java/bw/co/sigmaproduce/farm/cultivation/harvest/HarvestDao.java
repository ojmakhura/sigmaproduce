// license-header java merge-point
//
// Attention: Generated code! Do not modify by hand!
// Generated by SpringDao.vsl in andromda-spring-cartridge on $springUtils.date. Do not modify by hand!.
//
package bw.co.sigmaproduce.farm.cultivation.harvest;

import bw.co.sigmaproduce.farm.cultivation.Cultivation;
import java.io.File;
import java.time.LocalDate;
import java.util.Collection;

/**
 * TODO: Model Documentation for Harvest
 * @see Harvest
 */
public interface HarvestDao
{
    /**
     * This constant is used as a transformation flag; entities can be converted automatically into value objects
     * or other types, different methods in a class implementing this interface support this feature: look for
     * an <code>int</code> parameter called <code>transform</code>.
     * <p>
     * This specific flag denotes no transformation will occur.
     */
    public static final int TRANSFORM_NONE = 0;


    /**
     * Transforms the given results to a collection of {@link Harvest}
     * instances (this is useful when the returned results contains a row of data and you want just entities only).
     *
     * @param results the query results.
     */
    public void toEntities(final Collection<?> results);

    /**
     * This constant is used as a transformation flag; entities can be converted automatically into value objects
     * or other types, different methods in a class implementing this interface support this feature: look for
     * an <code>int</code> parameter called <code>transform</code>.
     * <p>
     * This specific flag denotes entities must be transformed into objects of type
     * {@link HarvestVO}.
     */
    public static final int TRANSFORM_HARVESTVO = 1;

    /**
     * Copies the fields of the specified entity to the target value object. This method is similar to
     * toHarvestVO(), but it does not handle any attributes in the target
     * value object that are "read-only" (as those do not have setter methods exposed).
     * @param source
     * @param target
     */
    public void toHarvestVO(
        Harvest source,
        HarvestVO target);

    /**
     * Converts this DAO's entity to an object of type {@link HarvestVO}.
     * @param entity
     * @return HarvestVO
     */
    public HarvestVO toHarvestVO(Harvest entity);

    /**
     * Converts this DAO's entity to a Collection of instances of type {@link HarvestVO}.
     * @param entities
     * @return Collection<HarvestVO>
     */
    public Collection<HarvestVO> toHarvestVOCollection(Collection<Harvest> entities);

    /**
     * Converts this DAO's entity to an array of instances of type {@link HarvestVO}.
     * @param entities
     * @return HarvestVO[]
     */
    public HarvestVO[] toHarvestVOArray(Collection<?> entities);

    /**
     * Copies the fields of {@link HarvestVO} to the specified entity.
     * @param source
     * @param target
     * @param copyIfNull If FALSE, the value object's field will not be copied to the entity if the value is NULL. If TRUE,
     * it will be copied regardless of its value.
     */
    public void harvestVOToEntity(
        HarvestVO source,
        Harvest target,
        boolean copyIfNull);

    /**
     * Converts an instance of type {@link HarvestVO} to this DAO's entity.
     * @param harvestVO
     * @return Harvest
     */
    public Harvest harvestVOToEntity(HarvestVO harvestVO);

    /**
     * Converts a Collection of instances of type {@link HarvestVO} to this
     * DAO's entity.
     * @param instances
     */
    public void harvestVOToEntityCollection(Collection<?> instances);

    /**
     * This constant is used as a transformation flag; entities can be converted automatically into value objects
     * or other types, different methods in a class implementing this interface support this feature: look for
     * an <code>int</code> parameter called <code>transform</code>.
     * <p>
     * This specific flag denotes entities must be transformed into objects of type
     * {@link HarvestList}.
     */
    public static final int TRANSFORM_HARVESTLIST = 2;

    /**
     * Copies the fields of the specified entity to the target value object. This method is similar to
     * toHarvestList(), but it does not handle any attributes in the target
     * value object that are "read-only" (as those do not have setter methods exposed).
     * @param source
     * @param target
     */
    public void toHarvestList(
        Harvest source,
        HarvestList target);

    /**
     * Converts this DAO's entity to an object of type {@link HarvestList}.
     * @param entity
     * @return HarvestList
     */
    public HarvestList toHarvestList(Harvest entity);

    /**
     * Converts this DAO's entity to a Collection of instances of type {@link HarvestList}.
     * @param entities
     * @return Collection<HarvestList>
     */
    public Collection<HarvestList> toHarvestListCollection(Collection<Harvest> entities);

    /**
     * Converts this DAO's entity to an array of instances of type {@link HarvestList}.
     * @param entities
     * @return HarvestList[]
     */
    public HarvestList[] toHarvestListArray(Collection<?> entities);

    /**
     * Copies the fields of {@link HarvestList} to the specified entity.
     * @param source
     * @param target
     * @param copyIfNull If FALSE, the value object's field will not be copied to the entity if the value is NULL. If TRUE,
     * it will be copied regardless of its value.
     */
    public void harvestListToEntity(
        HarvestList source,
        Harvest target,
        boolean copyIfNull);

    /**
     * Converts an instance of type {@link HarvestList} to this DAO's entity.
     * @param harvestList
     * @return Harvest
     */
    public Harvest harvestListToEntity(HarvestList harvestList);

    /**
     * Converts a Collection of instances of type {@link HarvestList} to this
     * DAO's entity.
     * @param instances
     */
    public void harvestListToEntityCollection(Collection<?> instances);

    /**
     * Gets an instance of Harvest from the persistent store.
     * @param id
     * @return Harvest
     */
    public Harvest get(String id);

    /**
     * <p>
     * Does the same thing as {@link #get(String)} with an
     * additional flag called <code>transform</code>. If this flag is set to <code>TRANSFORM_NONE</code> then
     * the returned entity will <strong>NOT</strong> be transformed. If this flag is any of the other constants
     * defined in this class then the result <strong>WILL BE</strong> passed through an operation which can
     * optionally transform the entity (into a value object for example). By default, transformation does
     * not occur.
     * </p>
     *
     * @param transform flag to determine transformation type.
     * @param id the identifier of the entity to get.
     * @return either the entity or the object transformed from the entity.
     */
    public Object get(int transform, String id);

    /**
     * Loads an instance of Harvest from the persistent store.
     * @param id
     * @return Harvest
     */
    public Harvest load(String id);

    /**
     * <p>
     * Does the same thing as {@link #load(String)} with an
     * additional flag called <code>transform</code>. If this flag is set to <code>TRANSFORM_NONE</code> then
     * the returned entity will <strong>NOT</strong> be transformed. If this flag is any of the other constants
     * defined in this class then the result <strong>WILL BE</strong> passed through an operation which can
     * optionally transform the entity (into a value object for example). By default, transformation does
     * not occur.
     * </p>
     *
     * @param transform flag to determine transformation type.
     * @param id the identifier of the entity to load.
     * @return either the entity or the object transformed from the entity.
     */
    public Object load(int transform, String id);

    /**
     * Loads all entities of type {@link Harvest}.
     *
     * @return the loaded entities.
     */
    public Collection<Harvest> loadAll();

    /**
     * <p>
     * Does the same thing as {@link #loadAll()} with an
     * additional flag called <code>transform</code>. If this flag is set to <code>TRANSFORM_NONE</code> then
     * the returned entity will <strong>NOT</strong> be transformed. If this flag is any of the other constants
     * defined here then the result <strong>WILL BE</strong> passed through an operation which can optionally
     * transform the entity (into a value object for example). By default, transformation does
     * not occur.
     * </p>
     *
     * @param transform the flag indicating what transformation to use.
     * @return the loaded entities.
     */
    public Collection<?> loadAll(final int transform);

    /**
     * <p>
     * Does the same thing as {@link #loadAll()} with an
     * additional two arguments called <code>pageNumber</code> and <code>pageSize</code>. The <code>pageNumber</code>
     * argument allows you to specify the page number when you are paging the results and the pageSize allows you to specify the size of the
     * page retrieved.
     * </p>
     *
     * @param pageNumber the page number to retrieve when paging results.
     * @param pageSize the size of the page to retrieve when paging results.
     * @return the loaded entities.
     */
    public Collection<?> loadAll(final int pageNumber, final int pageSize);

    /**
     * <p>
     * Does the same thing as {@link #loadAll(int)} with an
     * additional two arguments called <code>pageNumber</code> and <code>pageSize</code>. The <code>pageNumber</code>
     * argument allows you to specify the page number when you are paging the results and the pageSize allows you to specify the size of the
     * page retrieved.
     * </p>
     *
     * @param transform the flag indicating what transformation to use.
     * @param pageNumber the page number to retrieve when paging results.
     * @param pageSize the size of the page to retrieve when paging results.
     * @return the loaded entities.
     */
    public Collection<?> loadAll(final int transform, final int pageNumber, final int pageSize);

    /**
     * Creates an instance of Harvest and adds it to the persistent store.
     * @param harvest
     * @return Harvest
     */
    public Harvest create(Harvest harvest);

    /**
     * <p>
     * Does the same thing as {@link #create(Harvest)} with an
     * additional flag called <code>transform</code>. If this flag is set to <code>TRANSFORM_NONE</code> then
     * the returned entity will <strong>NOT</strong> be transformed. If this flag is any of the other constants
     * defined here then the result <strong>WILL BE</strong> passed through an operation which can optionally
     * transform the entity (into a value object for example). By default, transformation does
     * not occur.
     * </p>
     * @param transform
     * @param harvest
     * @return Object
     */
    public Object create(int transform, Harvest harvest);

    /**
     * Creates a new instance of Harvest and adds
     * from the passed in <code>entities</code> collection
     *
     * @param entities the collection of Harvest
     * instances to create.
     *
     * @return the created instances.
     */
    public Collection<Harvest> create(Collection<Harvest> entities);

    /**
     * <p>
     * Does the same thing as {@link #create(Harvest)} with an
     * additional flag called <code>transform</code>. If this flag is set to <code>TRANSFORM_NONE</code> then
     * the returned entity will <strong>NOT</strong> be transformed. If this flag is any of the other constants
     * defined here then the result <strong>WILL BE</strong> passed through an operation which can optionally
     * transform the entities (into value objects for example). By default, transformation does
     * not occur.
     * </p>
     * @param transform
     * @param entities
     * @return Collection
     */
    public Collection<?> create(int transform, Collection<Harvest> entities);

    /**
     * <p>
     * Creates a new <code>Harvest</code>
     * instance from <strong>all</strong> attributes and adds it to
     * the persistent store.
     * </p>
     * @param createdBy TODO: Model Documentation for Harvest.createdBy
     * @param harvestDate TODO: Model Documentation for Harvest.harvestDate
     * @param quantity TODO: Model Documentation for Harvest.quantity
     * @param unit TODO: Model Documentation for Harvest.unit
     * @param images TODO: Model Documentation for Harvest.images
     * @return Harvest
     */
    public Harvest create(
        String createdBy,
        LocalDate harvestDate,
        Integer quantity,
        HarvestUnit unit,
        Collection<File> images);

    /**
     * <p>
     * Does the same thing as {@link #create(String, LocalDate, Integer, HarvestUnit, File)} with an
     * additional flag called <code>transform</code>. If this flag is set to <code>TRANSFORM_NONE</code> then
     * the returned entity will <strong>NOT</strong> be transformed. If this flag is any of the other constants
     * defined here then the result <strong>WILL BE</strong> passed through an operation which can optionally
     * transform the entity (into a value object for example). By default, transformation does
     * not occur.
     * </p>
     * @param transform
     * @param createdBy TODO: Model Documentation for Harvest.createdBy
     * @param harvestDate TODO: Model Documentation for Harvest.harvestDate
     * @param quantity TODO: Model Documentation for Harvest.quantity
     * @param unit TODO: Model Documentation for Harvest.unit
     * @param images TODO: Model Documentation for Harvest.images
     * @return Harvest
     */
    public Object create(
        int transform,
        String createdBy,
        LocalDate harvestDate,
        Integer quantity,
        HarvestUnit unit,
        Collection<File> images);

    /**
     * <p>
     * Creates a new <code>Harvest</code>
     * instance from only <strong>required</strong> properties (attributes
     * and association ends) and adds it to the persistent store.
     * </p>
     * @param createdBy
     * @param cultivation
     * @param harvestDate
     * @param unit
     * @return Harvest
     */
    public Harvest create(
        String createdBy,
        Cultivation cultivation,
        LocalDate harvestDate,
        HarvestUnit unit);

    /**
     * <p>
     * Does the same thing as {@link #create(String, LocalDate, HarvestUnit)} with an
     * additional flag called <code>transform</code>. If this flag is set to <code>TRANSFORM_NONE</code> then
     * the returned entity will <strong>NOT</strong> be transformed. If this flag is any of the other constants
     * defined here then the result <strong>WILL BE</strong> passed through an operation which can optionally
     * transform the entity (into a value object for example). By default, transformation does
     * not occur.
     * </p>
     * @param transform flag to determine transformation type.
     * @param createdBy
     * @param cultivation
     * @param harvestDate
     * @param unit
     * @return Object
     */
    public Object create(
        int transform,
        String createdBy,
        Cultivation cultivation,
        LocalDate harvestDate,
        HarvestUnit unit);

    /**
     * Updates the <code>harvest</code> instance in the persistent store.
     * @param harvest
     */
    public void update(Harvest harvest);

    /**
     * Create or Update the <code>harvest</code> instance in the persistent store.
     * @param harvest
     * @return Harvest
     */
    public Harvest createOrUpdate(Harvest harvest);

    /**
     * Updates all instances in the <code>entities</code> collection in the persistent store.
     * @param entities
     */
    public void update(Collection<Harvest> entities);

    /**
     * Removes the instance of Harvest from the persistent store.
     * @param harvest
     */
    public void remove(Harvest harvest);

    /**
     * Removes the instance of Harvest having the given
     * <code>identifier</code> from the persistent store.
     * @param id
     */
    public void remove(String id);

    /**
     * Removes all entities in the given <code>entities</code> collection.
     * @param entities
     */
    public void remove(Collection<Harvest> entities);


    /**
     * Allows transformation of entities into value objects
     * (or something else for that matter), when the <code>transform</code>
     * flag is set to one of the constants defined in <code>bw.co.sigmaproduce.farm.cultivation.harvest.HarvestDao</code>, please note
     * that the {@link #TRANSFORM_NONE} constant denotes no transformation, so the entity itself
     * will be returned.
     * <p>
     * This method will return instances of these types:
     * <ul>
     *   <li>{@link Harvest} - {@link #TRANSFORM_NONE}</li>
     *   <li>{@link HarvestVO} - {@link #TRANSFORM_HARVESTVO}</li>
     *   <li>{@link HarvestList} - {@link #TRANSFORM_HARVESTLIST}</li>
     * </ul>
     *
     * If the integer argument value is unknown {@link #TRANSFORM_NONE} is assumed.
     *
     * @param transform one of the constants declared in {@link bw.co.sigmaproduce.farm.cultivation.harvest.HarvestDao}
     * @param entity an entity that was found
     * @return the transformed entity (i.e. new value object, etc)
     * @see #transformEntities(int,Collection)
     */
    public Object transformEntity(final int transform, final Harvest entity);

    /**
     * Transforms a collection of entities using the
     * {@link #transformEntity(int,Harvest)}
     * method. This method does not instantiate a new collection.
     * <p>
     * This method is to be used internally only.
     *
     * @param transform one of the constants declared in <code>bw.co.sigmaproduce.farm.cultivation.harvest.HarvestDao</code>
     * @param entities the collection of entities to transform
     * @see #transformEntity(int,Harvest)
     */
    public void transformEntities(final int transform, final Collection<?> entities);

    /**
     * Searches for a single instance of Harvest.
     * @param images the unique images to be used in the search.'
     * @return a single instance of Harvest.
     */
    public Harvest searchUniqueImages(final File images);

    /**
     * Searches for a single instance of Harvest.
     * @param transform the transformation flag.
     * @param images the unique images to be used in the search.
     * @return a value object according the transformation flag.
     */
    public Object searchUniqueImages(final int transform, final File images);


    // spring-dao merge-point
}
// license-header java merge-point
//
// Attention: Generated code! Do not modify by hand!
// Generated by SpringServiceBase.vsl in andromda-spring-cartridge on $springUtils.date. Do not modify by hand!.
//
/**
 * TEMPLATE:    SpringServiceBase.vsl in andromda-spring cartridge
 * MODEL CLASS: AndroMDAModel::backend::bw.co.sigmaproduce::farm::cultivation::problem::CultivationProblemService
 * STEREOTYPE:  Service
 */
package bw.co.sigmaproduce.farm.cultivation.problem;

import bw.co.sigmaproduce.PrincipalStore;
import java.security.Principal;
import java.util.Collection;
import java.util.Locale;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.context.MessageSource;
import org.springframework.data.domain.Page;

/**
 * <p>
 * Spring Service base class for <code>CultivationProblemService</code>,
 * provides access to all services and entities referenced by this service.
 * </p>
 *
 * @see CultivationProblemService
 */
public abstract class CultivationProblemServiceBase
    implements CultivationProblemService
{
    protected Logger logger = LoggerFactory.getLogger(CultivationProblemService.class);
    protected final CultivationProblemDao cultivationProblemDao;
    protected final CultivationProblemRepository cultivationProblemRepository;
    protected final MessageSource messageSource;

    public CultivationProblemServiceBase(
        CultivationProblemDao cultivationProblemDao,
        CultivationProblemRepository cultivationProblemRepository,
        MessageSource messageSource
    ) {
        this.cultivationProblemDao = cultivationProblemDao;
        this.cultivationProblemRepository = cultivationProblemRepository;
        this.messageSource = messageSource;
    }

	/**
     * Gets the reference to <code>cultivationProblem</code>'s DAO.
     * @return cultivationProblemDao
     */
    protected CultivationProblemDao getCultivationProblemDao()
    {
        return this.cultivationProblemDao;
    }

    /**
     * Gets the reference to <code>cultivationProblem</code>'s repository.
     * @return cultivationProblemRepository
     */
    public CultivationProblemRepository getCultivationProblemRepository() {
        return cultivationProblemRepository;
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public CultivationProblemVO findById(final String id)
    {
        if (id == null || id.trim().length() == 0)
        {
            throw new IllegalArgumentException(
                "bw.co.sigmaproduce.farm.cultivation.problem.CultivationProblemService.findById(String id) - 'id' can not be null or empty");
        }
        try
        {
            return this.handleFindById(id);
        }
        catch (Throwable th)
        {
            throw new CultivationProblemServiceException(
                "Error performing 'CultivationProblemService.findById(String id)' --> " + th,
                th);
        }
    }

    /**
     * Performs the core logic for {@link #findById(String)}
     * @param id String TODO: Model Documentation for
CultivationProblemService.findById(id)
     * @return CultivationProblemVO
     * @throws Exception
     */
    protected abstract CultivationProblemVO handleFindById(String id)
        throws Exception;

    /**
     * {@inheritDoc}
     */
    @Override
    public Collection<CultivationProblemListVO> getAll()
    {
        try
        {
            return this.handleGetAll();
        }
        catch (Throwable th)
        {
            throw new CultivationProblemServiceException(
                "Error performing 'CultivationProblemService.getAll()' --> " + th,
                th);
        }
    }

    /**
     * Performs the core logic for {@link #getAll()}
     * @return Collection<CultivationProblemListVO>
     * @throws Exception
     */
    protected abstract Collection<CultivationProblemListVO> handleGetAll()
        throws Exception;

    /**
     * {@inheritDoc}
     */
    @Override
    public boolean remove(final String id)
    {
        if (id == null || id.trim().length() == 0)
        {
            throw new IllegalArgumentException(
                "bw.co.sigmaproduce.farm.cultivation.problem.CultivationProblemService.remove(String id) - 'id' can not be null or empty");
        }
        try
        {
            return this.handleRemove(id);
        }
        catch (Throwable th)
        {
            throw new CultivationProblemServiceException(
                "Error performing 'CultivationProblemService.remove(String id)' --> " + th,
                th);
        }
    }

    /**
     * Performs the core logic for {@link #remove(String)}
     * @param id String TODO: Model Documentation for
CultivationProblemService.remove(id)
     * @return boolean
     * @throws Exception
     */
    protected abstract boolean handleRemove(String id)
        throws Exception;

    /**
     * {@inheritDoc}
     */
    @Override
    public CultivationProblemVO save(final CultivationProblemVO cultivationProblem)
    {
        if (cultivationProblem == null)
        {
            throw new IllegalArgumentException(
                "bw.co.sigmaproduce.farm.cultivation.problem.CultivationProblemService.save(CultivationProblemVO cultivationProblem) - 'cultivationProblem' can not be null");
        }
        if (cultivationProblem.getCropIssue() == null)
        {
            throw new IllegalArgumentException(
                "bw.co.sigmaproduce.farm.cultivation.problem.CultivationProblemService.save(CultivationProblemVO cultivationProblem) - 'cultivationProblem.cropIssue' can not be null");
        }
        if (cultivationProblem.getCultivation() == null)
        {
            throw new IllegalArgumentException(
                "bw.co.sigmaproduce.farm.cultivation.problem.CultivationProblemService.save(CultivationProblemVO cultivationProblem) - 'cultivationProblem.cultivation' can not be null");
        }
        if (cultivationProblem.getDescription() == null || cultivationProblem.getDescription().trim().length() == 0)
        {
            throw new IllegalArgumentException(
                "bw.co.sigmaproduce.farm.cultivation.problem.CultivationProblemService.save(CultivationProblemVO cultivationProblem) - 'cultivationProblem.description' can not be null or empty");
        }
        if (cultivationProblem.getDateNoticed() == null)
        {
            throw new IllegalArgumentException(
                "bw.co.sigmaproduce.farm.cultivation.problem.CultivationProblemService.save(CultivationProblemVO cultivationProblem) - 'cultivationProblem.dateNoticed' can not be null");
        }
        try
        {
            return this.handleSave(cultivationProblem);
        }
        catch (Throwable th)
        {
            throw new CultivationProblemServiceException(
                "Error performing 'CultivationProblemService.save(CultivationProblemVO cultivationProblem)' --> " + th,
                th);
        }
    }

    /**
     * Performs the core logic for {@link #save(CultivationProblemVO)}
     * @param cultivationProblem CultivationProblemVO TODO: Model Documentation for
CultivationProblemService.save(cultivationProblem)
     * @return CultivationProblemVO
     * @throws Exception
     */
    protected abstract CultivationProblemVO handleSave(CultivationProblemVO cultivationProblem)
        throws Exception;

    /**
     * {@inheritDoc}
     */
    @Override
    public Collection<CultivationProblemListVO> search(final String criteria)
    {
        try
        {
            return this.handleSearch(criteria);
        }
        catch (Throwable th)
        {
            throw new CultivationProblemServiceException(
                "Error performing 'CultivationProblemService.search(String criteria)' --> " + th,
                th);
        }
    }

    /**
     * Performs the core logic for {@link #search(String)}
     * @param criteria String TODO: Model Documentation for
CultivationProblemService.search(criteria)
     * @return Collection<CultivationProblemListVO>
     * @throws Exception
     */
    protected abstract Collection<CultivationProblemListVO> handleSearch(String criteria)
        throws Exception;

    /**
     * {@inheritDoc}
     */
    @Override
    public Page<CultivationProblemListVO> search(final Integer pageNumber, final Integer pageSize, final String criteria)
    {
        if (criteria == null || criteria.trim().length() == 0)
        {
            throw new IllegalArgumentException(
                "bw.co.sigmaproduce.farm.cultivation.problem.CultivationProblemService.search(Integer pageNumber, Integer pageSize, String criteria) - 'criteria' can not be null or empty");
        }
        try
        {
            return this.handleSearch(pageNumber, pageSize, criteria);
        }
        catch (Throwable th)
        {
            throw new CultivationProblemServiceException(
                "Error performing 'CultivationProblemService.search(Integer pageNumber, Integer pageSize, String criteria)' --> " + th,
                th);
        }
    }

    /**
     * Performs the core logic for {@link #search(Integer, Integer, String)}
     * @param pageNumber Integer TODO: Model Documentation for
CultivationProblemService.search(pageNumber)
     * @param pageSize Integer TODO: Model Documentation for
CultivationProblemService.search(pageSize)
     * @param criteria String TODO: Model Documentation for
CultivationProblemService.search(criteria)
     * @return Page<CultivationProblemListVO>
     * @throws Exception
     */
    protected abstract Page<CultivationProblemListVO> handleSearch(Integer pageNumber, Integer pageSize, String criteria)
        throws Exception;

    /**
     * {@inheritDoc}
     */
    @Override
    public Page<CultivationProblemListVO> getAll(final Integer pageNumber, final Integer pageSize)
    {
        try
        {
            return this.handleGetAll(pageNumber, pageSize);
        }
        catch (Throwable th)
        {
            throw new CultivationProblemServiceException(
                "Error performing 'CultivationProblemService.getAll(Integer pageNumber, Integer pageSize)' --> " + th,
                th);
        }
    }

    /**
     * Performs the core logic for {@link #getAll(Integer, Integer)}
     * @param pageNumber Integer TODO: Model Documentation for
CultivationProblemService.getAll(pageNumber)
     * @param pageSize Integer TODO: Model Documentation for
CultivationProblemService.getAll(pageSize)
     * @return Page<CultivationProblemListVO>
     * @throws Exception
     */
    protected abstract Page<CultivationProblemListVO> handleGetAll(Integer pageNumber, Integer pageSize)
        throws Exception;

    /**
     * Gets the current <code>principal</code> if one has been set,
     * otherwise returns <code>null</code>.
     *
     * @return the current principal
     */
    protected Principal getPrincipal()
    {
        return PrincipalStore.get();
    }

    /**
     * Gets the message source available to this service.
     * @return MessageSource
     */
    protected MessageSource getMessages()
    {
        return messageSource;
    }

    /**
     * Gets the message having the given <code>key</code> in the underlying message bundle.
     *
     * @param key the key of the message in the messages.properties message bundle.
     * @return String
     */
    protected String getMessage(final String key)
    {
        return this.getMessages().getMessage(key, null, null);
    }

    /**
     * Gets the message having the given <code>key</code> and <code>arguments</code> in the
     * underlying message bundle.
     *
     * @param key the key of the message in the messages.properties message bundle.
     * @param arguments any arguments to substitute when resolving the message.
     * @return String
     */
    protected String getMessage(final String key, final Object[] arguments)
    {
        return this.getMessages().getMessage(key, arguments, null);
    }

    /**
     * Gets the message having the given <code>key</code> using the given <code>arguments</code>
     * for the given <code>locale</code>.
     *
     * @param key the key of the message in the messages.properties message bundle.
     * @param arguments any arguments to substitute when resolving the message.
     * @param locale the locale of the messages to retrieve.
     * @return String
     */
    protected String getMessage(
        final String key, final Object[] arguments,
        final Locale locale)
    {
        return this.getMessages().getMessage(key, arguments, locale);
    }


// spring-service-base merge-point
}
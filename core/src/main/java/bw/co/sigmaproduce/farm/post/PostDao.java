// license-header java merge-point
//
// Attention: Generated code! Do not modify by hand!
// Generated by SpringDao.vsl in andromda-spring-cartridge on $springUtils.date. Do not modify by hand!.
//
package bw.co.sigmaproduce.farm.post;

import java.io.File;
import java.time.LocalDateTime;
import java.util.Collection;
import org.springframework.data.domain.Page;

/**
 * TODO: Model Documentation for Post
 * @see Post
 */
public interface PostDao
{
    /**
     * This constant is used as a transformation flag; entities can be converted automatically into value objects
     * or other types, different methods in a class implementing this interface support this feature: look for
     * an <code>int</code> parameter called <code>transform</code>.
     * <p>
     * This specific flag denotes no transformation will occur.
     */
    public static final int TRANSFORM_NONE = 0;


    /**
     * Transforms the given results to a collection of {@link Post}
     * instances (this is useful when the returned results contains a row of data and you want just entities only).
     *
     * @param results the query results.
     */
    public void toEntities(final Collection<?> results);

    /**
     * This constant is used as a transformation flag; entities can be converted automatically into value objects
     * or other types, different methods in a class implementing this interface support this feature: look for
     * an <code>int</code> parameter called <code>transform</code>.
     * <p>
     * This specific flag denotes entities must be transformed into objects of type
     * {@link PostVO}.
     */
    public static final int TRANSFORM_POSTVO = 1;

    /**
     * Copies the fields of the specified entity to the target value object. This method is similar to
     * toPostVO(), but it does not handle any attributes in the target
     * value object that are "read-only" (as those do not have setter methods exposed).
     * @param source
     * @param target
     */
    public void toPostVO(
        Post source,
        PostVO target);

    /**
     * Converts this DAO's entity to an object of type {@link PostVO}.
     * @param entity
     * @return PostVO
     */
    public PostVO toPostVO(Post entity);

    /**
     * Converts this DAO's entity to a Collection of instances of type {@link PostVO}.
     * @param entities
     * @return Collection<PostVO>
     */
    public Collection<PostVO> toPostVOCollection(Collection<Post> entities);

    /**
     * Converts this DAO's entity to an array of instances of type {@link PostVO}.
     * @param entities
     * @return PostVO[]
     */
    public PostVO[] toPostVOArray(Collection<?> entities);

    /**
     * Copies the fields of {@link PostVO} to the specified entity.
     * @param source
     * @param target
     * @param copyIfNull If FALSE, the value object's field will not be copied to the entity if the value is NULL. If TRUE,
     * it will be copied regardless of its value.
     */
    public void postVOToEntity(
        PostVO source,
        Post target,
        boolean copyIfNull);

    /**
     * Converts an instance of type {@link PostVO} to this DAO's entity.
     * @param postVO
     * @return Post
     */
    public Post postVOToEntity(PostVO postVO);

    /**
     * Converts a Collection of instances of type {@link PostVO} to this
     * DAO's entity.
     * @param instances
     */
    public void postVOToEntityCollection(Collection<?> instances);

    /**
     * Gets an instance of Post from the persistent store.
     * @param id
     * @return Post
     */
    public Post get(String id);

    /**
     * <p>
     * Does the same thing as {@link #get(String)} with an
     * additional flag called <code>transform</code>. If this flag is set to <code>TRANSFORM_NONE</code> then
     * the returned entity will <strong>NOT</strong> be transformed. If this flag is any of the other constants
     * defined in this class then the result <strong>WILL BE</strong> passed through an operation which can
     * optionally transform the entity (into a value object for example). By default, transformation does
     * not occur.
     * </p>
     *
     * @param transform flag to determine transformation type.
     * @param id the identifier of the entity to get.
     * @return either the entity or the object transformed from the entity.
     */
    public Object get(int transform, String id);

    /**
     * Loads an instance of Post from the persistent store.
     * @param id
     * @return Post
     */
    public Post load(String id);

    /**
     * <p>
     * Does the same thing as {@link #load(String)} with an
     * additional flag called <code>transform</code>. If this flag is set to <code>TRANSFORM_NONE</code> then
     * the returned entity will <strong>NOT</strong> be transformed. If this flag is any of the other constants
     * defined in this class then the result <strong>WILL BE</strong> passed through an operation which can
     * optionally transform the entity (into a value object for example). By default, transformation does
     * not occur.
     * </p>
     *
     * @param transform flag to determine transformation type.
     * @param id the identifier of the entity to load.
     * @return either the entity or the object transformed from the entity.
     */
    public Object load(int transform, String id);

    /**
     * Loads all entities of type {@link Post}.
     *
     * @return the loaded entities.
     */
    public Collection<Post> loadAll();

    /**
     * <p>
     * Does the same thing as {@link #loadAll()} with an
     * additional flag called <code>transform</code>. If this flag is set to <code>TRANSFORM_NONE</code> then
     * the returned entity will <strong>NOT</strong> be transformed. If this flag is any of the other constants
     * defined here then the result <strong>WILL BE</strong> passed through an operation which can optionally
     * transform the entity (into a value object for example). By default, transformation does
     * not occur.
     * </p>
     *
     * @param transform the flag indicating what transformation to use.
     * @return the loaded entities.
     */
    public Collection<?> loadAll(final int transform);

    /**
     * <p>
     * Does the same thing as {@link #loadAll()} with an
     * additional two arguments called <code>pageNumber</code> and <code>pageSize</code>. The <code>pageNumber</code>
     * argument allows you to specify the page number when you are paging the results and the pageSize allows you to specify the size of the
     * page retrieved.
     * </p>
     *
     * @param pageNumber the page number to retrieve when paging results.
     * @param pageSize the size of the page to retrieve when paging results.
     * @return the loaded entities.
     */
    public Collection<?> loadAll(final int pageNumber, final int pageSize);

    /**
     * <p>
     * Does the same thing as {@link #loadAll(int)} with an
     * additional two arguments called <code>pageNumber</code> and <code>pageSize</code>. The <code>pageNumber</code>
     * argument allows you to specify the page number when you are paging the results and the pageSize allows you to specify the size of the
     * page retrieved.
     * </p>
     *
     * @param transform the flag indicating what transformation to use.
     * @param pageNumber the page number to retrieve when paging results.
     * @param pageSize the size of the page to retrieve when paging results.
     * @return the loaded entities.
     */
    public Collection<?> loadAll(final int transform, final int pageNumber, final int pageSize);

    /**
     * Creates an instance of Post and adds it to the persistent store.
     * @param post
     * @return Post
     */
    public Post create(Post post);

    /**
     * <p>
     * Does the same thing as {@link #create(Post)} with an
     * additional flag called <code>transform</code>. If this flag is set to <code>TRANSFORM_NONE</code> then
     * the returned entity will <strong>NOT</strong> be transformed. If this flag is any of the other constants
     * defined here then the result <strong>WILL BE</strong> passed through an operation which can optionally
     * transform the entity (into a value object for example). By default, transformation does
     * not occur.
     * </p>
     * @param transform
     * @param post
     * @return Object
     */
    public Object create(int transform, Post post);

    /**
     * Creates a new instance of Post and adds
     * from the passed in <code>entities</code> collection
     *
     * @param entities the collection of Post
     * instances to create.
     *
     * @return the created instances.
     */
    public Collection<Post> create(Collection<Post> entities);

    /**
     * <p>
     * Does the same thing as {@link #create(Post)} with an
     * additional flag called <code>transform</code>. If this flag is set to <code>TRANSFORM_NONE</code> then
     * the returned entity will <strong>NOT</strong> be transformed. If this flag is any of the other constants
     * defined here then the result <strong>WILL BE</strong> passed through an operation which can optionally
     * transform the entities (into value objects for example). By default, transformation does
     * not occur.
     * </p>
     * @param transform
     * @param entities
     * @return Collection
     */
    public Collection<?> create(int transform, Collection<Post> entities);

    /**
     * <p>
     * Creates a new <code>Post</code>
     * instance from <strong>all</strong> attributes and adds it to
     * the persistent store.
     * </p>
     * @param createdBy TODO: Model Documentation for Post.createdBy
     * @param updatedBy TODO: Model Documentation for Post.updatedBy
     * @param updatedDate TODO: Model Documentation for Post.updatedDate
     * @param createdDate TODO: Model Documentation for Post.createdDate
     * @param content TODO: Model Documentation for Post.content
     * @param images TODO: Model Documentation for Post.images
     * @return Post
     */
    public Post create(
        String createdBy,
        String updatedBy,
        LocalDateTime updatedDate,
        LocalDateTime createdDate,
        String content,
        Collection<File> images);

    /**
     * <p>
     * Does the same thing as {@link #create(String, String, LocalDateTime, LocalDateTime, String, File)} with an
     * additional flag called <code>transform</code>. If this flag is set to <code>TRANSFORM_NONE</code> then
     * the returned entity will <strong>NOT</strong> be transformed. If this flag is any of the other constants
     * defined here then the result <strong>WILL BE</strong> passed through an operation which can optionally
     * transform the entity (into a value object for example). By default, transformation does
     * not occur.
     * </p>
     * @param transform
     * @param createdBy TODO: Model Documentation for Post.createdBy
     * @param updatedBy TODO: Model Documentation for Post.updatedBy
     * @param updatedDate TODO: Model Documentation for Post.updatedDate
     * @param createdDate TODO: Model Documentation for Post.createdDate
     * @param content TODO: Model Documentation for Post.content
     * @param images TODO: Model Documentation for Post.images
     * @return Post
     */
    public Object create(
        int transform,
        String createdBy,
        String updatedBy,
        LocalDateTime updatedDate,
        LocalDateTime createdDate,
        String content,
        Collection<File> images);


    /**
     * Updates the <code>post</code> instance in the persistent store.
     * @param post
     */
    public void update(Post post);

    /**
     * Create or Update the <code>post</code> instance in the persistent store.
     * @param post
     * @return Post
     */
    public Post createOrUpdate(Post post);

    /**
     * Updates all instances in the <code>entities</code> collection in the persistent store.
     * @param entities
     */
    public void update(Collection<Post> entities);

    /**
     * Removes the instance of Post from the persistent store.
     * @param post
     */
    public void remove(Post post);

    /**
     * Removes the instance of Post having the given
     * <code>identifier</code> from the persistent store.
     * @param id
     */
    public void remove(String id);

    /**
     * Removes all entities in the given <code>entities</code> collection.
     * @param entities
     */
    public void remove(Collection<Post> entities);

    /**
     * TODO: Model Documentation for Post.findFarmPosts
     * @param farmId TODO: Model Documentation for Post.findFarmPosts(farmId)
     * @return Collection<Post>
     */
    public Collection<Post> findFarmPosts(String farmId);

    /**
     * TODO: Model Documentation for Post.findFarmPosts
     * @param farmId TODO: Model Documentation for Post.findFarmPosts(farmId)
     * @param pageSize TODO: Model Documentation for Post.findFarmPosts(pageSize)
     * @param pageNumber TODO: Model Documentation for Post.findFarmPosts(pageNumber)
     * @return Page<Post>
     */
    public Page<Post> findFarmPosts(String farmId, Integer pageSize, Integer pageNumber);


    /**
     * Allows transformation of entities into value objects
     * (or something else for that matter), when the <code>transform</code>
     * flag is set to one of the constants defined in <code>bw.co.sigmaproduce.farm.post.PostDao</code>, please note
     * that the {@link #TRANSFORM_NONE} constant denotes no transformation, so the entity itself
     * will be returned.
     * <p>
     * This method will return instances of these types:
     * <ul>
     *   <li>{@link Post} - {@link #TRANSFORM_NONE}</li>
     *   <li>{@link PostVO} - {@link #TRANSFORM_POSTVO}</li>
     * </ul>
     *
     * If the integer argument value is unknown {@link #TRANSFORM_NONE} is assumed.
     *
     * @param transform one of the constants declared in {@link bw.co.sigmaproduce.farm.post.PostDao}
     * @param entity an entity that was found
     * @return the transformed entity (i.e. new value object, etc)
     * @see #transformEntities(int,Collection)
     */
    public Object transformEntity(final int transform, final Post entity);

    /**
     * Transforms a collection of entities using the
     * {@link #transformEntity(int,Post)}
     * method. This method does not instantiate a new collection.
     * <p>
     * This method is to be used internally only.
     *
     * @param transform one of the constants declared in <code>bw.co.sigmaproduce.farm.post.PostDao</code>
     * @param entities the collection of entities to transform
     * @see #transformEntity(int,Post)
     */
    public void transformEntities(final int transform, final Collection<?> entities);


    // spring-dao merge-point
}
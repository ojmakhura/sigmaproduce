// license-header java merge-point
//
// Attention: Generated code! Do not modify by hand!
// Generated by SpringWS.java.vsl in andromda-webservices. Do not modify by hand!.
//
package bw.co.sigmaproduce.farm.cultivation.harvest;

import io.swagger.v3.oas.annotations.Operation;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.DeleteMapping;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.PostMapping;
import org.springframework.web.bind.annotation.RequestBody;

/**
 * 
 * TODO: Model Documentation for bw.co.sigmaproduce.farm.cultivation.harvest.HarvestApi
 */
public interface HarvestApi {

    @GetMapping("/{id}")
    @Operation(summary = "Find Farm by id", description = "Get the farm with the given id")
    public ResponseEntity<?> findById(@PathVariable(name = "id", required = true) String id);

    @GetMapping()
    @Operation(summary = "Get Harvests", description = "Get all harvests in the data store")
    public ResponseEntity<?> getAll();

    @GetMapping("/page/{pageNumber}/size/{pageSize}")
    @Operation(summary = "Get Harvest Page", description = "Get {pageSize} harvests with for a particular {pageNumber}")
    public ResponseEntity<?> getAllPaged(@PathVariable(name = "pageNumber", required = false) Integer pageNumber, @PathVariable(name = "pageSize", required = false) Integer pageSize);

    @PostMapping("/search/page/{pageNumber}/size/{pageSize}")
    @Operation(summary = "Search Harvest Page", description = "Get {pageSize} harves for a particular {pageNumber} given a search criteria")
    public ResponseEntity<?> pagedSearch(@RequestBody(required = true) HarvestCriteria criteria, @PathVariable(name = "pageNumber", required = false) Integer pageNumber, @PathVariable(name = "pageSize", required = false) Integer pageSize);

    @DeleteMapping("/{id}")
    @Operation(summary = "Remove Harvest by id", description = "Remove the harvest with the given id")
    public ResponseEntity<?> remove(@PathVariable(name = "id", required = true) String id);

    @PostMapping()
    @Operation(summary = "Save Harvest", description = "Save a harvest to data store")
    public ResponseEntity<?> save(@RequestBody(required = true) HarvestVO harvest);

    @PostMapping("search")
    @Operation(summary = "Search Harvests", description = "Search harvests based on the criteria")
    public ResponseEntity<?> search(@RequestBody(required = false) HarvestCriteria criteria);
}
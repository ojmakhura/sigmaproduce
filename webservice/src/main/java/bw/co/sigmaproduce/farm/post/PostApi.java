// license-header java merge-point
//
// Attention: Generated code! Do not modify by hand!
// Generated by SpringWS.java.vsl in andromda-webservices. Do not modify by hand!.
//
package bw.co.sigmaproduce.farm.post;

import io.swagger.v3.oas.annotations.Operation;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.DeleteMapping;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.PostMapping;
import org.springframework.web.bind.annotation.RequestBody;
import org.springframework.web.bind.annotation.RequestParam;

/**
 * 
 * TODO: Model Documentation for bw.co.sigmaproduce.farm.post.PostApi
 */
public interface PostApi {

    @GetMapping("/{id}")
    @Operation(summary = "Find Post by id", description = "Get the post with the given id")
    public ResponseEntity<?> findById(@PathVariable(name = "id", required = true) String id);

    @GetMapping("byFarm")
    public ResponseEntity<?> findFarmPosts(@RequestParam(name = "farmId", required = true) String farmId);

    @GetMapping("byFarm/paged")
    public ResponseEntity<?> findFarmPostsPaged(@PathVariable(name = "farmId", required = true) String farmId, @PathVariable(name = "pageSize", required = false) Integer pageSize, @PathVariable(name = "pageNumber", required = false) Integer pageNumber);

    @GetMapping()
    @Operation(summary = "Get Farms", description = "Get all farms in the data store")
    public ResponseEntity<?> getAll();

    @DeleteMapping("/{id}")
    @Operation(summary = "Remove farm by id", description = "Remove the farm with the given id")
    public ResponseEntity<?> remove(@PathVariable(name = "id", required = true) String id);

    @PostMapping()
    @Operation(summary = "Save Farm", description = "Save a farm to data store")
    public ResponseEntity<?> save(@RequestBody(required = true) PostVO post);

    @GetMapping("search")
    @Operation(summary = "Search Farms", description = "Search farms based on the criteria")
    public ResponseEntity<?> search(@RequestParam(name = "criteria", required = false) String criteria);
}